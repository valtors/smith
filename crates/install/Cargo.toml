[package]
name = "smith-mcp-install"
description = "server installation for smith MCP server manager"
license = "MIT"
repository = "https://github.com/valtors/smith"
version = "0.1.0"
edition = "2021"

[dependencies]
smith-mcp-config = { path = "../config", version = "0.1.0" }
