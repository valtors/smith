[package]
name = "smith-mcp-secure"
description = "security auditing for smith MCP server manager"
license = "MIT"
repository = "https://github.com/valtors/smith"
version = "0.1.0"
edition = "2021"

[dependencies]
smith-mcp-config = { path = "../config", version = "0.1.0" }
serde = { workspace = true }
serde_json = { workspace = true }
