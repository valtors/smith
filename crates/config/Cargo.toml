[package]
name = "smith-mcp-config"
description = "config management for smith MCP server manager"
license = "MIT"
repository = "https://github.com/valtors/smith"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
